language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-yo-sf2-vagrant' ]; then cd .. && eval "mv $currentfolder generator-yo-sf2-vagrant" && cd generator-yo-sf2-vagrant; fi

